services:
  - type: web
    name: ai-research-agent
    runtime: python

    buildCommand: |
      pip install uv
      uv sync

    startCommand: uv run uvicorn api:app --host 0.0.0.0 --port $PORT

    plan: free